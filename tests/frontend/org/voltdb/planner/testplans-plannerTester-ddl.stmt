select * from t where a = ? limit ?;
select * from t where a > ? limit ?;
select * from t where a < ? limit ?;
select * from t where a > ? and a < ? limit ?;
select * from t where a = ? limit ?;
select * from l, t where l.b=t.b limit ?;
select * from t order by a limit ?;