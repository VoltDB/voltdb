drop table T if exists;
CREATE TABLE T (
  A INTEGER,
  B INTEGER,
  C INTEGER
);

drop table R1 if exists;

CREATE TABLE R1 (
  ID INTEGER NOT NULL,
  TINY INTEGER NOT NULL,
  BIG INTEGER NOT NULL,
  PRIMARY KEY (ID)
);
