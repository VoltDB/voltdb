CREATE TABLE FAKE_ACCOUNTS
(
  ID INTEGER NOT NULL,
  NAME VARCHAR(10) NOT NULL,
  BALANCE BIGINT NOT NULL,
  PRIMARY KEY (ID)
);

CREATE TABLE EMPTY_TABLE
(
  ID INTEGER NOT NULL,
  PRIMARY KEY (ID)
);
