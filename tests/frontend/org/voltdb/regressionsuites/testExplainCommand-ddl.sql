
CREATE TABLE t1 (
 PKEY          INTEGER NOT NULL,
 A_INT         INTEGER,
 A_STR  VARCHAR(10),
 PRIMARY KEY (PKEY)
);

PARTITION TABLE t1 ON COLUMN PKEY;

CREATE TABLE t2 (
 PKEY          INTEGER NOT NULL,
 A_INT         INTEGER,
 A_STR  VARCHAR(10),
 PRIMARY KEY (PKEY)
);

CREATE TABLE t3 (
 PKEY INTEGER NOT NULL,
 I3  INTEGER NOT NULL,
 I4  INTEGER NOT NULL,
 PRIMARY KEY (PKEY)
 );

PARTITION TABLE t3 ON COLUMN PKEY;

CREATE INDEX t3_TREE ON t3 (I3 DESC);

CREATE INDEX t3_PLUS ON t3 (I3 + I4);
