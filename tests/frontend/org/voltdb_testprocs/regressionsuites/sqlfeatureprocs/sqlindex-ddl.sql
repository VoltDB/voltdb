CREATE TABLE T1 (
	ID INTEGER NOT NULL,
	POINTS INTEGER NOT NULL,
	PRIMARY KEY (ID)
);
create unique index idx_1_COUNTER on T1 (POINTS);
	
CREATE TABLE T2 (
	ID INTEGER NOT NULL,
	NAME VARCHAR(10) NOT NULL,
	POINTS INTEGER NOT NULL,
	PRIMARY KEY (ID)
);
create unique index idx_2_COUNTER on T2 (ID,POINTS);

CREATE TABLE T3 (
	ID INTEGER NOT NULL,
	NAME VARCHAR(10) NOT NULL,
	POINTS INTEGER NOT NULL,
	PRIMARY KEY (ID)
);
create unique index idx_3_COUNTER on T2 (NAME,POINTS);