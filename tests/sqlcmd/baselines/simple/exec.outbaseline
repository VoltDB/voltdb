drop table t if exists;
create table t ( c integer, d varchar(3) );
exec T.insert (1,'a');
execute T.insert 2 ab
exec T.insert 3,'abc'
execute T.insert,4,'abcd'

-- extra whitespace
 exec T.insert (1, 'a' );
  execute T.insert 2 ab
exec    T.insert 3 , 'abc' 
execute  T.insert  ,  4  ,  'abcd'

-- tabs for fun
exec T.insert (1, 'a' );
  execute T.insert2ab
exec   T.insert 3,'abc'
executeT.insert,4,'abcd'

select * from t;
drop table t;

