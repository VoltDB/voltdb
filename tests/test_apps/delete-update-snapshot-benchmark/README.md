# delete-update-snapshot-benchmark aka DUSBenchmark

-----------------------------------------------------------------------------
## build and push the docker container

1. build the product
2. build the jars
3. run docker_build.sh
