We're currently using Boost 1.70

It's header-only and we specifically disable any references to boost::thread.

No included Boost files have been modified. We keep all the library headers in, and exclude all components that
needs building artifacts (e.g. thread, filesystem, etc.).

